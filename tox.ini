[tox]
envlist = py37, py38, py39, lint

[testenv]
deps =
    pytest
commands =
    pytest

[testenv:lint]
deps = 
    black
    flake8 
    isort 
commands = 
    isort --check pystv tests
    flake8 pystv tests
    black --check pystv tests


